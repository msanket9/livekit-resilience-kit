# Pinned exactly -- see client/requirements.txt for why. livekit core is left
# to livekit-agents to resolve so the two stay compatible.
livekit-agents==1.7.1
livekit-plugins-silero==1.7.1
numpy==2.5.2
